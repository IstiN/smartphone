<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <View
        android:id="@+id/shadow"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0"
        android:background="#000000"
        android:clickable="true"
        android:focusable="true" />

    <include
        android:id="@+id/search_bar_container"
        layout="@layout/view_search" />

        <android.support.v7.widget.RecyclerView
            android:id="@android:id/list"
            android:layout_below="@id/search_bar_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="@dimen/default_margin_plus_card"
            android:layout_marginRight="@dimen/default_margin_plus_card"
            android:layout_marginBottom="@dimen/default_margin_4"
            />


</RelativeLayout>